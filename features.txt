banking-api

--------- feature 1: open account --- DONE!!!

account-api
	POST /account/open

customer-api
	rpc Create(CustomerRequest) returns (CustomerReply);

--------- feature 2: make a transaction --- DONE!!!

transaction-api
	POST /account/{id}/transaction
	POST /account/{id}/transfer/{id}/{amount}

account-api
	rpc ProcessAmount(UpdateRequest) returns (UpdateReply);
	rpc TransferAmount(TransferRequest) returns (TransferReply);

--------- feature 3: get account statement
bugs:
    account-api: include balance in account response
    customer-api: log customer id in customer grpc service

account-api
	GET  /account/{id}/statement

customer-api
	rpc FindById(google.protobuf.Int64Value) returns (CustomerReply);

transaction-api
	rpc FindByAccountId(google.protobuf.Int64Value) returns (stream TransactionReply);


--------- feature 4: get customer position

customer-api
	GET /customer/all

account-api
	rpc FindByCustomerId(google.protobuf.Int64Value) returns (stream AccountReply);

